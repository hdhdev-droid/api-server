# 서버
PORT=3000

# DB (배포 시 설정) - DB_TYPE: POSTGRESQL | MYSQL | MARIADB | MONGODB
DB_TYPE=
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
# MongoDB 전용 (선택) - 없으면 DB_HOST/DB_PORT/DB_NAME/DB_USER/DB_PASSWORD 로 연결
DB_URI=
